<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css?family=Roboto+Condensed:700" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lato:700" rel="stylesheet">
    <title>Libreria UQ</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

    <body>
        <div class="menu-container" data-sticky-container>
            <div class="title-bar" data-sticky data-options="marginTop:0;" style="width:100%" data-top-anchor="1" data-btm-anchor="content:bottom">
                <div class="title-bar-left">
                    <%= image_tag("uniquindio.png", :alt => "uniquindio_img", :class => "uniquindio") %>
                </div>
                <div class="title-bar-right menu-derecho">
                    <ul class="dropdown menu align-right" data-dropdown-menu>                        
                        <li>
                            <a href="#">Productos</a>
                        </li>
                        <li>
                            <a href="#">Carrito</a>
                        </li>
                        <% if not usuario_signed_in? %>
                            <li>
                                <a href="#">Log In</a>
                                
                            </li>
                        <% else %>
                            <li>
                                <a href="#"> <%= link_to 'Cerrar Sesion', destroy_usuario_session_path, :method => :delete %></a>
                            </li>
                        <% end %>
                    </ul>   
                </div>
            </div>
        </div>
        <%= yield %>

    </body>
</html>
