<div class="row align-center" style="margin-bottom: 80px;">
	<div class="columns text-right large-2 icono_recuperar" style=" padding-top:8px; color: #2d6324;">
		<i class="fa fa-expeditedssl fa-3x"></i>
	</div>
	<div class="columns large-5" style="margin-left:15px;">
		<h2>Recuperación</h2>
	</div>

</div>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field" style="margin: 20px auto;">
  	<div class="row">
  		<div class="columns text-right large-4 icono_email" style="color: #2d6324;" ;">
  			<i class="fa fa-envelope fa-2x"></i>
  		</div>
  		<div class="columns large-4" style="padding-left: 15px;">
  			<%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Digite su email" %>
  		</div>
  	</div>
  </div>
	<div class="actions">
		<div class="row align-center">
			<div class="columns large-2">
				<%= f.submit "Enviar instrucciones", :class => "hollow button secondary" %>
			</div>
		</div>
	</div>
<% end %>
	<div class="recuperar_link_registrate">
		<%= render "devise/shared/links" %>		
	</div>
